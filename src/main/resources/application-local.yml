spring:
  datasource:
    url: jdbc:mysql://localhost:3307/realtime_chat?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: realtime_chat
    password: realtime_chat

  flyway:
    validate-on-migrate: false

jwt:
  secret: local-secret-key-must-be-at-least-32-bytes-long
  access-token-validity-seconds: 3600
  refresh-token-validity-seconds: 1209600
  refresh-cookie-secure: false
